<!DOCTYPE etl SYSTEM "http://scriptella.org/dtd/etl.dtd">
<etl>
    <connection driver="org.h2.Driver"
                url="jdbc:h2:mem:jdbc_public_value_flow;MODE=LEGACY;NON_KEYWORDS=VALUE"
                user="sa" password=""/>

    <query>
        SELECT ID, NAME, AMOUNT FROM Source ORDER BY ID
        <script>
            INSERT INTO Target (ID, NAME, AMOUNT)
            VALUES (?{ID}, ?{NAME}, ?{AMOUNT});
        </script>
    </query>
</etl>
