<!DOCTYPE etl SYSTEM "http://scriptella.org/dtd/etl.dtd">
<etl>
    <properties>
        <include href="${properties.file}"/>
    </properties>
    <connection driver="${db.driver}" url="${db.url}"
                user="${db.user}" password="${db.password}"
                classpath="${db.classpath}"/>
    <script>
        <include href="${sql.file}"/>
    </script>
</etl>
