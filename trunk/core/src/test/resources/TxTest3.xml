<!DOCTYPE scripts SYSTEM "scriptella.dtd">
<scripts>
    <connection driver="org.hsqldb.jdbcDriver" url="jdbc:hsqldb:mem:txtest3" user="sa" password=""/>
    <script>
        CREATE TABLE Test (
        ID INT NOT NULL PRIMARY KEY,
        VALUE CHAR(11)
        );
        insert into test(id, value) values (1,'111');
        insert into test(id, value) values (2,'222');
        CREATE TABLE Test2 (
        ID INT,
        VALUE CHAR(11)
        );
    </script>

    <query>
        select * from Test
        <script new-tx="true"><!--Should fail after 1st iteration-->
            insert into Test2(id, value) values (1,$VALUE);
        </script>
    </query>

</scripts>